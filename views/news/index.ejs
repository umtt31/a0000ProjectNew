<% layout('./layouts/boilerplate') %>
<h1>News</h1>
<div>
    <a href="/news/new">Add a new</a>
</div>
<% for (let report of reports) { %>
    <div class="card mb-3">
        <div class="row">
            <div class="col-md-4">
                <img class="card-img-top image-fluid" src="<%= report.image %>" alt="">
            </div>
            <div class="col-md-8">
                <div class="card-body">
                    <h5 class="card-title"><%= report.title %></h5>
                    <p class="card-text"><%= report.description %></p>
                    <p class="card-text">
                        <small class="text-muted"><%= report.publicationDate %></small>
                    </p>
                    <a  class="btn btn-primary" href="/news/<%= report._id %>">View <%= report.title %></a>
                </div>  
            </div>
        </div>
    </div>
<% } %>